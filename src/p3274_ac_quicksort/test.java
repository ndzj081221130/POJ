package p3274_ac_quicksort;import java.awt.datatransfer.StringSelection;public class test {	/**	 * @param args	 */	public static void main(String[] args) {		// TODO Auto-generated method stub//		int x = 15;//		String Selection = Integer.toBinaryString(x);//		System.out.println(Selection);		int[] arr = {4,3,2,5,31};		QuickSort(arr,0,4);		for (int i = 0; i < arr.length; i++) {			System.out.print(arr[i]+" ");		}	}	private static void QuickSort(int[] array,int left,int right) {		if (left < right) {			int pivotpos = partition(array,left,right);			QuickSort(array, left, pivotpos-1);			QuickSort(array, pivotpos+1, right);		}	}	private static int partition(int[] array, int left, int right) {		int pivotpos = left;		int pivot = array[left];		for (int i = left+1; i <= right; i++) {			if (array[i] < pivot) {				pivotpos++;				if (pivotpos != i) {					Swap(array,pivotpos,i);									}			}					}		array[left] = array[pivotpos];		array[pivotpos] = pivot;		return pivotpos;	}	private static void Swap(int[] array, int pivotpos, int in) {		int temp = array[in];		array[in] = array[pivotpos];		array[pivotpos] = temp;		System.out.println("= -");		for (int i = 0; i < array.length; i++) {			System.out.print(array[i]+" ");		}	}}